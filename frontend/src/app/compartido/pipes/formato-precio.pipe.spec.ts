import { FormatoPrecioPipe } from './formato-precio.pipe';

describe('FormatoPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
