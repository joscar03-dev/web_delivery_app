import { EfectoHoverDirective } from './efecto-hover.directive';

describe('EfectoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new EfectoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
